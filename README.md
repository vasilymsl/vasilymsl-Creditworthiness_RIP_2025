# Разработка интернет-приложений (РИП) 2025

## 🏦 Система оценки кредитоспособности

Разработанное приложение позволяет врачам создавать и формировать заявки на расчеты кредитоспособности для пациентов, а главным врачам завершать или отклонять данные заявки.

---

## 📦 Репозитории проекта

### **Лабораторная работа 8: Асинхронный сервис**

| Компонент | Репозиторий | Ветка | Описание |
|-----------|-------------|-------|----------|
| 🐍 **Django Async** | [lab8-django](https://github.com/vasilymsl/lab8-django) | `lab8_django` | Асинхронный сервис с задержкой 5-10 сек и callback |
| ⚛️ **React Frontend** | [lab8-react](https://github.com/vasilymsl/lab8-react) | `lab8_front` | Модераторский интерфейс, short polling, QR-коды |
| 🔷 **Go Backend** | [lab8-golang](https://github.com/vasilymsl/lab8-golang) | `lab8_golang` | REST API с псевдо-токеном и async endpoints |

---

## 🎯 Реализованный функционал (ЛР8)

### **Асинхронный сервис (Django)**
- ✅ HTTP метод с задержкой 5-10 секунд
- ✅ Обновление поля `total_amount` в таблице `application`
- ✅ Callback в основной сервис через HTTP
- ✅ Без прямого доступа к БД (только через API)

### **Основной сервис (Go)**
- ✅ HTTP метод для приема результатов от async сервиса
- ✅ Псевдо-авторизация через `X-Lab8-Token` (8 байт)
- ✅ REST API endpoints: `/api/async/applications/:id/result`
- ✅ Инициация async обработки при завершении заявки

### **Модераторский интерфейс (React)**
- ✅ Кнопки изменения статусов заявок (`complete`, `reject`)
- ✅ Фильтры по дате формирования (диапазон)
- ✅ Фильтры по статусу заявки (backend)
- ✅ Фильтр по создателю заявки (frontend)
- ✅ Short polling (обновление каждые 2 сек) с умной оптимизацией

### **Дополнительное задание**
- ✅ QR-код на странице каждой услуги
- ✅ ZeroTier для локальной сети (Mac ↔ iPhone)
- ✅ Открытие страницы на телефоне по QR

---

## 🛠 Технологический стек

### Backend
- **Go 1.21+** — основной веб-сервис
  - Gin (REST API)
  - GORM (ORM)
  - PostgreSQL
  - Redis
  - MinIO (S3-совместимое хранилище)
- **Python 3.14** — асинхронный сервис
  - Django 4.2
  - Django Rest Framework
  - ThreadPoolExecutor (async tasks)

### Frontend
- **React 18** — пользовательский интерфейс
  - Redux Toolkit (state management)
  - React-Bootstrap (UI компоненты)
  - Vite (bundler)
  - QRCode.react (генерация QR-кодов)

### Инфраструктура
- **Docker & Docker Compose**
- **Nginx** (прокси для MinIO)
- **ZeroTier** (виртуальная сеть)

---

## 🚀 Запуск проекта

### 1️⃣ Клонирование репозиториев

```bash
# Django async service
git clone git@github.com:vasilymsl/lab8-django.git
cd lab8-django
git checkout lab8_django

# React frontend
git clone git@github.com:vasilymsl/lab8-react.git
cd lab8-react
git checkout lab8_front

# Go backend
git clone git@github.com:vasilymsl/lab8-golang.git
cd lab8_golang
git checkout lab8_golang
```

### 2️⃣ Запуск через Docker Compose

```bash
cd lab8-golang
docker-compose up -d
```

### 3️⃣ Запуск Django async service

```bash
cd lab8-django
pip install -r requirements.txt
python manage.py runserver 0.0.0.0:8090
```

### 4️⃣ Запуск React frontend

```bash
cd lab8-react
npm install
npm run dev -- --port 3001
```

---

## 📡 API Endpoints

### Асинхронные операции

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| `POST` | `/set_result` | Запуск async обработки (Django) | - |
| `GET` | `/api/async/applications/:id/input` | Получение входных данных (Go) | - |
| `PUT` | `/api/async/applications/:id/result` | Прием результата от Django | `X-Lab8-Token` |
| `PUT` | `/api/async/applications/:id/override` | Переопределение результата | `X-Lab8-Token` |
| `POST` | `/api/async/applications/:id/start` | Ручной запуск async | JWT + Moderator |

### Модераторские действия

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| `PUT` | `/api/applications/:id` | Изменение статуса заявки | JWT + Moderator |
| `GET` | `/api/applications` | Список заявок с фильтрами | JWT |

---

## 📸 Скриншоты

- ✅ Insomnia: async запросы с токенами
- ✅ Django: логи с задержкой 6 сек
- ✅ React: модераторский интерфейс
- ✅ Wireshark: захват трафика на порту 3001
- ✅ ZeroTier: сеть с Mac и iPhone
- ✅ QR-код: генерация и сканирование

---

## 👨‍💻 Автор

**Васильев Василий Маслович** (ИУ5-54б)  
МГТУ им. Н.Э. Баумана, 2025

---

## 📄 Лицензия

Учебный проект в рамках курса "Разработка интернет-приложений"